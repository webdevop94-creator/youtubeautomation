{
  "name": "scene3d",
  "private": true,
  "description": "Headless three.js renderer for the cartoon scenes. node_modules used to be a symlink into a temp directory, which would have taken the whole 3D pipeline with it the first time Windows cleaned that up; this file is how it gets reinstalled if it is ever lost.",
  "scripts": {
    "render": "node render.js"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.5",
    "puppeteer": "^24.0.0"
  }
}
